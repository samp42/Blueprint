//
//  Transpose.cpp
//  Blueprint
//
//  Created by Samuel Proulx on 2020-02-09.
//  Copyright © 2020 SP. All rights reserved.
//

/**
 * Function to transpose a matrix
 * Transpose an array representation of a sparse matrix
*/

/**
 * 
 * vector<[row<short>, column<short>, value<float>]>
*/

#include <iostream>
#include <vector>

template<class T>{
    std::vector<[T]> MultiplyM(std::vector<[T]> MatrixA, std::vector<[T]> MatrixB){

        array ResultMatrix = new <T>[MatrixA.size()][MatrixB[0].size()]; //array = rows of A x columns of B
        for(int i in MatrixA[0].size() - 1){
            for(int j in MatrixA.size() - 1){
                if()
            }
        }
        return ResultMatrix;
    }
}