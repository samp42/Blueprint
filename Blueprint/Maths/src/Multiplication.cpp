//
//  Multiplication.cpp
//  Blueprint
//
//  Created by Samuel Proulx on 2020-02-08.
//  Copyright © 2020 SP. All rights reserved.
//

/**
 * Function to multiply matrices
 * Multiplies the two elements and puts it into an array representation of a sparse matrix
*/

#include <iostream>
#include <vector>

template<class T>;
std::vector<std::vector<T>> MultiplyM(std::vector<std::vector<T>> MatrixA, std::vector<std::vector<T>> MatrixB){
    array ResultMatrix = new <T>[MatrixA.size()][MatrixB[0].size()]; //array = rows of A x columns of B
    
    for(int i in MatrixA[0].size()){
        for(int j in MatrixA.size()){
            if()
        }
    }
    return ResultMatrix;
}
