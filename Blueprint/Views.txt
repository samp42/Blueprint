Interface main layout:
    -MainMenu (left) -unchanged depending on view
    -MainSection (center) -supports content
        Prints: "No event selected." if no event is selected
    -RightMenu (right) -changes depending on view
	-InfoView (bottom right) - changes depending on team

Score Board:
    -ScoreTableView
    -RankingView
    -AutoRankingView
    -LowerPortRankingView
    -OuterPortRankingView
    -InnerPortRankingView
    -ClimbRankingView
    -PowerRankingView

Teams:
    -TeamsListView
    -TeamView (example list cell)
    -PitScoutingView
    -MatchesView
    -StatsView

